apiVersion: v1
kind: Service
metadata:
  name: {{ include "dtrack-publisher-chart.fullname" . }}
  labels:
    {{- include "dtrack-publisher-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dtrack-publisher-chart.selectorLabels" . | nindent 4 }}
